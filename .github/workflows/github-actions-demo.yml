# name: GitHub Actions Demo
# run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
# on: [push]
# jobs:
#   Explore-GitHub-Actions:
#     runs-on: ubuntu-latest
#     environment: ENV_TEST
#     env:
#       HELLO: "Hello"
#     steps:
#       - run: echo " ${{ env.HELLO }} và mysql pass${{ secrets.MYSQL_PASSWORD }} và ${{ secrets.TEST }}  event."
#       - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
#       - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
#       - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
#       - name: Check out repository code
#         uses: actions/checkout@v5
#       - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
#       - run: echo "🖥️ The workflow is now ready to test your code on the runner."
#       - name: List files in the repository
#         run: |
#           ls ${{ github.workspace }}
#       - name: Create a new file
#         run : touch github-action-file.txt
#       - name: Again list files in the repository
#         run: |
#           ls ${{ github.workspace }}
#       - run: echo "🍏 This job's status is ${{ job.status }}."
#   test-2:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "This is the second job."
#       - run: echo "This job's status is ${{ job.status }}."
name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
# jobs:
#   job1: # Tên job này là 'job1'
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v5
#       - run: mkdir build && echo "hello" > build/test.txt # Tạo thử 1 file

#       - name: Upload Artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: my-packages # Đặt tên ở đây
#           path: build/      # Thư mục muốn gửi đi
#   job2:
#     needs: job1 # QUAN TRỌNG: Bảo Job 2 đợi Job 1 làm xong đã!
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v5

#       - name: Download Artifact
#         uses: actions/download-artifact@v4
#         with:
#           name: my-packages # Phải trùng khớp 100% tên ở trên
#           path: build/      # Nơi muốn tải về máy 2

#       - run: cat build/test.txt # Kiểm tra xem file đã sang chưa
jobs:
  start-job:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run : mkdir -p /test-thien
      - run : echo "hello" >> /test-thien/test.txt
      - run : npm install
      - run: npm run test-hello
      - run: docker build -t my-node-app .
      - run: docker run -d --name my-node-app my-node-app
